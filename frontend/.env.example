# Backend origin (no trailing slash). Omit or leave empty in local dev to use the Vite proxy
# (same-origin cookies with OAuth redirect to http://localhost:8000/... in GCP).
# Set explicitly e.g. to http://localhost:8000 if you are not using `vite.config` proxy.
# VITE_API_BASE_URL=
