"""
Application package root for the Calendar Assistant FastAPI backend.

Holds the ASGI app (`main`), configuration, routers, DB layer, Google integrations,
LangGraph agent, and Pydantic schemas. No business logic in this `__init__` file.
"""
